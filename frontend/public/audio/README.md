# Memorial story background music

Place a calm, royalty-free ambient track here as `memorial-ambient.mp3`.

The story player loads `/audio/memorial-ambient.mp3` first, then falls back to a hosted preview if the file is missing.

Recommended: instrumental piano or soft strings, 2–4 minutes, loop-friendly.
