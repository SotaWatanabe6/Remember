"use client";

export default function MemorialLayout({ children }) {
  // TODO: Add sidebar + header for memorial space shell
  // This is the container for all memorial subroutes
  return <>{children}</>;
}
