// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   turbopack: {
//     root: new URL(".", import.meta.url).pathname,
//   },
// };

// export default nextConfig;

/** 
 * @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;